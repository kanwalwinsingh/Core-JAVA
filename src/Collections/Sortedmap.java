package Collections;

import java.util.NavigableMap;
import java.util.SortedMap;

public class Sortedmap {

	public static void main(String[] args) {
		SortedMap map; 
		NavigableMap navigableMap; 
		
		/**
		 * Navigable Map methods - 
		 * 
		 * 1. headMap = Return view of map whose keys are less than the given key. Take one parameter.
		 * 2. tailMap = Return view of map whose keys are greater than or equal to  the given key. Take one parameter.
		 * 3. subMap = Take 2 parameter from and to and return that map
		 */

	}

}
